---
description: Run the full local verification pass CI also runs
---

Run `pnpm check` and report each step's result in order. Stop at the first failure and report the
exact command and output, never a paraphrase. A red step is not summarised as a pass.
